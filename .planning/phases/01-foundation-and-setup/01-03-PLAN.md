---
phase: 01-foundation-and-setup
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/hooks/useAuth.ts
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/components/ProtectedRoute.tsx
  - frontend/components.json
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "React 19 app builds successfully with Vite 8 and TypeScript"
    - "shadcn/ui is initialized with Tailwind v4 and slate theme"
    - "Login page renders with Spanish copy per UI-SPEC"
    - "JWT token is stored in localStorage after successful login"
    - "ProtectedRoute redirects to /login when no token exists"
    - "Vite dev server proxies /api requests to backend at localhost:8000"
  artifacts:
    - path: "frontend/package.json"
      provides: "Node project with all Phase 1 frontend dependencies"
      contains: "react"
    - path: "frontend/vite.config.ts"
      provides: "Vite 8 config with Tailwind v4 plugin and API proxy"
      contains: "@tailwindcss/vite"
    - path: "frontend/src/App.tsx"
      provides: "React Router + TanStack Query providers + route definitions"
      contains: "BrowserRouter"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Login form with Spanish copy matching UI-SPEC"
      contains: "Iniciar sesion"
    - path: "frontend/src/components/ProtectedRoute.tsx"
      provides: "JWT auth guard redirecting to /login"
      contains: "Navigate"
    - path: "frontend/src/lib/api.ts"
      provides: "Axios instance with JWT interceptor"
      contains: "Authorization"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/LoginPage.tsx"
      via: "react-router Route"
      pattern: "LoginPage"
    - from: "frontend/src/lib/api.ts"
      to: "/api/auth/login"
      via: "axios baseURL + interceptor"
      pattern: "/api"
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "Vite proxy config"
      pattern: "proxy.*8000"
---

<objective>
Scaffold the React 19 frontend with Vite 8, TypeScript, Tailwind v4, shadcn/ui, and implement the login page with JWT authentication. After this plan, the frontend builds, shadcn/ui components are available, and users can log in.

Purpose: The frontend shell and auth flow must exist before the onboarding wizard (Plan 05) can be built.
Output: Working React SPA with login page, JWT auth hooks, axios API client, and routing skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-setup/01-RESEARCH.md
@.planning/phases/01-foundation-and-setup/01-UI-SPEC.md
@.planning/phases/01-foundation-and-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React 19 + Vite 8 + TypeScript project with Tailwind v4, shadcn/ui, and all dependencies</name>
  <files>
    frontend/package.json,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/vite.config.ts,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/src/vite-env.d.ts,
    frontend/components.json
  </files>
  <read_first>
    .planning/phases/01-foundation-and-setup/01-RESEARCH.md,
    .planning/phases/01-foundation-and-setup/01-UI-SPEC.md,
    frontend/Dockerfile
  </read_first>
  <action>
Initialize the frontend project inside the `frontend/` directory:

**Step 1: Create Vite React TypeScript project**
```bash
cd frontend
npm create vite@latest . -- --template react-ts
```
If the directory has existing files (Dockerfile, nginx.conf), the CLI may prompt — accept overwrite for generated files only.

**Step 2: Install all Phase 1 dependencies**
```bash
npm install react-router @tanstack/react-query react-hook-form zod axios lucide-react
```

**Step 3: Install Tailwind CSS v4 with Vite plugin**
```bash
npm install tailwindcss @tailwindcss/vite
```

**Step 4: Initialize shadcn/ui**
```bash
npx shadcn@latest init
```
When prompted: use default style (New York or Default), slate base color, CSS variables yes. The CLI will update/create `components.json`, modify `src/index.css`, and potentially create `src/lib/utils.ts`.

**Step 5: Add required shadcn components**
```bash
npx shadcn@latest add button card form input label progress separator badge alert
```

**Step 6: Configure vite.config.ts** — Must include Tailwind v4 plugin and API proxy:
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:8000",
        changeOrigin: true,
      },
    },
  },
});
```

**Step 7: Configure src/index.css** — Tailwind v4 import (CSS-first, no tailwind.config.js):
Ensure the file starts with:
```css
@import "tailwindcss";
```
shadcn/ui init may add CSS variables below this import — keep those.

**Step 8: Update index.html** — Set Spanish lang and title:
```html
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DIAN Facturador</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 9: Minimal App.tsx** — Placeholder with routing skeleton (will be fully built in next task):
```tsx
import { BrowserRouter, Routes, Route } from "react-router";
import { QueryClientProvider } from "@tanstack/react-query";
import { queryClient } from "@/lib/queryClient";

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <Routes>
          <Route path="*" element={<div>DIAN Facturador</div>} />
        </Routes>
      </BrowserRouter>
    </QueryClientProvider>
  );
}

export default App;
```

**Step 10: Create src/lib/queryClient.ts**:
```typescript
import { QueryClient } from "@tanstack/react-query";

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: 1,
      staleTime: 5 * 60 * 1000, // 5 minutes
    },
  },
});
```

Verify the build works: `cd frontend && npm run build`
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5 && echo "BUILD_SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains `"react"` as a dependency
    - frontend/package.json contains `"react-router"`
    - frontend/package.json contains `"@tanstack/react-query"`
    - frontend/package.json contains `"react-hook-form"`
    - frontend/package.json contains `"zod"`
    - frontend/package.json contains `"axios"`
    - frontend/package.json contains `"lucide-react"`
    - frontend/package.json contains `"tailwindcss"`
    - frontend/package.json contains `"@tailwindcss/vite"`
    - frontend/vite.config.ts contains `@tailwindcss/vite`
    - frontend/vite.config.ts contains `proxy`
    - frontend/vite.config.ts contains `localhost:8000`
    - frontend/src/index.css contains `@import "tailwindcss"`
    - frontend/index.html contains `lang="es"`
    - frontend/index.html contains `DIAN Facturador`
    - frontend/components.json exists (shadcn/ui initialized)
    - `npm run build` succeeds in the frontend directory
  </acceptance_criteria>
  <done>React 19 + Vite 8 + TypeScript project initialized. Tailwind v4 with Vite plugin configured. shadcn/ui initialized with slate theme and all Phase 1 components added. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement login page, JWT auth hook, axios API client, and protected route</name>
  <files>
    frontend/src/lib/api.ts,
    frontend/src/hooks/useAuth.ts,
    frontend/src/pages/LoginPage.tsx,
    frontend/src/components/ProtectedRoute.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-and-setup/01-UI-SPEC.md,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/components.json
  </read_first>
  <action>
**frontend/src/lib/api.ts** — Axios instance with JWT interceptor:
```typescript
import axios from "axios";

const api = axios.create({
  baseURL: "/api",
  headers: {
    "Content-Type": "application/json",
  },
});

// Request interceptor: attach JWT token
api.interceptors.request.use((config) => {
  const token = localStorage.getItem("access_token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Response interceptor: redirect to login on 401
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem("access_token");
      window.location.href = "/login";
    }
    return Promise.reject(error);
  }
);

export default api;
```

**frontend/src/hooks/useAuth.ts** — Auth state hook:
```typescript
import { useState, useCallback } from "react";
import api from "@/lib/api";

interface LoginCredentials {
  username: string;
  password: string;
}

export function useAuth() {
  const [isAuthenticated, setIsAuthenticated] = useState<boolean>(
    () => !!localStorage.getItem("access_token")
  );

  const login = useCallback(async (credentials: LoginCredentials) => {
    const response = await api.post("/auth/login", credentials);
    const { access_token } = response.data;
    localStorage.setItem("access_token", access_token);
    setIsAuthenticated(true);
    return response.data;
  }, []);

  const logout = useCallback(() => {
    localStorage.removeItem("access_token");
    setIsAuthenticated(false);
  }, []);

  return { isAuthenticated, login, logout };
}
```

**frontend/src/components/ProtectedRoute.tsx** — JWT guard:
```tsx
import { Navigate, Outlet } from "react-router";

export function ProtectedRoute() {
  const token = localStorage.getItem("access_token");
  return token ? <Outlet /> : <Navigate to="/login" replace />;
}
```

**frontend/src/pages/LoginPage.tsx** — Full login page per UI-SPEC:
The login page must follow the UI-SPEC layout contract exactly:
- Centered Card (max-w-sm, w-full, p-6)
- Display heading: "DIAN Facturador" (text-2xl font-semibold)
- Body text: "Ingresa para gestionar tu facturacion electronica" (text-sm text-muted-foreground)
- Separator
- Form with React Hook Form + Zod validation:
  - Input field: "Usuario" label
  - Password field: "Contrasena" label with Eye/EyeOff toggle (lucide-react icons, 16px)
    - Toggle aria-label: "Mostrar contrasena" when hidden, "Ocultar contrasena" when visible
  - Button: "Iniciar sesion" full-width, primary variant
  - Loading state: Loader2 spinner with animate-spin, button disabled
- Alert (destructive): "Usuario o contrasena incorrectos. Vuelve a intentarlo." shown on 401

Use shadcn/ui components: Card, CardHeader, CardContent, Button, Input, Label, Separator, Alert, AlertDescription.
Use lucide-react: Eye, EyeOff, Loader2.
Use React Hook Form with Zod schema:
```typescript
const loginSchema = z.object({
  username: z.string().min(1, "Este campo es obligatorio."),
  password: z.string().min(1, "Este campo es obligatorio."),
});
```

On successful login, redirect to "/" using react-router's `useNavigate()`.

**Update frontend/src/App.tsx** — Full routing:
```tsx
import { BrowserRouter, Routes, Route, Navigate } from "react-router";
import { QueryClientProvider } from "@tanstack/react-query";
import { queryClient } from "@/lib/queryClient";
import { ProtectedRoute } from "@/components/ProtectedRoute";
import LoginPage from "@/pages/LoginPage";

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <Routes>
          <Route path="/login" element={<LoginPage />} />
          <Route element={<ProtectedRoute />}>
            {/* SetupGuard and onboarding routes will be added in Plan 05 */}
            <Route path="/" element={<div className="p-8">Dashboard stub - Plan 05</div>} />
          </Route>
          <Route path="*" element={<Navigate to="/" replace />} />
        </Routes>
      </BrowserRouter>
    </QueryClientProvider>
  );
}

export default App;
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5 && grep -q "Iniciar" src/pages/LoginPage.tsx && grep -q "ProtectedRoute" src/App.tsx && grep -q "Authorization" src/lib/api.ts && echo "LOGIN_PAGE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api.ts contains `baseURL: "/api"`
    - frontend/src/lib/api.ts contains `Authorization`
    - frontend/src/lib/api.ts contains `Bearer`
    - frontend/src/lib/api.ts contains `localStorage.getItem("access_token")`
    - frontend/src/hooks/useAuth.ts contains `localStorage.setItem("access_token"`
    - frontend/src/hooks/useAuth.ts contains `api.post("/auth/login"`
    - frontend/src/components/ProtectedRoute.tsx contains `Navigate to="/login"`
    - frontend/src/pages/LoginPage.tsx contains `DIAN Facturador`
    - frontend/src/pages/LoginPage.tsx contains `Iniciar`
    - frontend/src/pages/LoginPage.tsx contains `Eye`
    - frontend/src/pages/LoginPage.tsx contains `Loader2`
    - frontend/src/pages/LoginPage.tsx contains `aria-label`
    - frontend/src/pages/LoginPage.tsx contains `useForm`
    - frontend/src/App.tsx contains `ProtectedRoute`
    - frontend/src/App.tsx contains `LoginPage`
    - frontend/src/App.tsx contains `BrowserRouter`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Login page renders with all Spanish copy per UI-SPEC. JWT auth flow works (login stores token, ProtectedRoute checks token). Axios instance attaches Bearer token to all requests. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds without errors
- LoginPage.tsx contains all Spanish copy strings from UI-SPEC
- ProtectedRoute redirects to /login when no token
- Vite proxy configured for /api -> localhost:8000
- shadcn components.json exists (shadcn/ui initialized)
- All required shadcn components installed (button, card, form, input, label, progress, separator, badge, alert)
</verification>

<success_criteria>
1. Frontend builds successfully with Vite 8 + React 19 + TypeScript
2. Tailwind v4 configured with CSS-first approach (@import "tailwindcss")
3. shadcn/ui initialized with all Phase 1 components
4. Login page matches UI-SPEC layout and copy exactly
5. JWT auth hook stores/clears token in localStorage
6. ProtectedRoute guards authenticated routes
7. Axios interceptor attaches JWT and redirects on 401
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-setup/01-03-SUMMARY.md`
</output>
