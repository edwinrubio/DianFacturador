---
phase: 02-data-catalogs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/dane.py
  - backend/app/models/unit_measure.py
  - backend/app/schemas/dane.py
  - backend/app/schemas/unit_measure.py
  - backend/app/routers/dane.py
  - backend/app/routers/units.py
  - backend/app/main.py
  - backend/alembic/versions/seed_dane_and_units.py
autonomous: true
requirements: [CATL-03]

must_haves:
  truths:
    - "GET /api/dane/departments returns 33 departments with code and name"
    - "GET /api/dane/municipalities?department_code=05 returns municipalities filtered by department"
    - "GET /api/dane/municipalities?q=med returns municipalities matching search text"
    - "GET /api/units returns all seeded unit of measure codes"
    - "DANE codes are stored as strings preserving leading zeros"
    - "Consumidor Final client exists in the database after migration"
  artifacts:
    - path: "backend/app/models/dane.py"
      provides: "Department and Municipality ORM models"
    - path: "backend/app/models/unit_measure.py"
      provides: "UnitOfMeasure ORM model"
    - path: "backend/app/routers/dane.py"
      provides: "DANE geographic lookup endpoints"
    - path: "backend/app/routers/units.py"
      provides: "Unit of measure list endpoint"
    - path: "backend/alembic/versions/seed_dane_and_units.py"
      provides: "Alembic migration with all seed data"
  key_links:
    - from: "backend/app/routers/dane.py"
      to: "backend/app/models/dane.py"
      via: "SQLAlchemy select queries"
      pattern: "select\\(Municipality\\)"
    - from: "backend/app/main.py"
      to: "backend/app/routers/dane.py"
      via: "include_router"
      pattern: "include_router.*dane_router"
---

<objective>
Create DANE geographic reference tables, unit of measure reference table, and seed all reference data via Alembic migration. Also seed the "Consumidor Final" default client record.

Purpose: Plans 02-02 and 02-03 depend on these reference tables for foreign key validation and dropdown data. This must run first.
Output: Working API endpoints for DANE lookups and unit of measure listing, plus all seed data in the database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-catalogs/02-CONTEXT.md
@.planning/phases/02-data-catalogs/02-RESEARCH.md

<interfaces>
<!-- Existing codebase patterns the executor needs -->

From backend/app/models/base.py:
```python
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass
```

From backend/app/core/database.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
```

From backend/app/core/security.py:
```python
async def get_current_user(...) -> User:
```

From backend/app/main.py (router registration pattern):
```python
from app.routers.resolutions import router as resolutions_router
app.include_router(resolutions_router)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DANE and UnitOfMeasure models + schemas + routers</name>
  <files>
    backend/app/models/dane.py
    backend/app/models/unit_measure.py
    backend/app/schemas/dane.py
    backend/app/schemas/unit_measure.py
    backend/app/routers/dane.py
    backend/app/routers/units.py
    backend/app/main.py
  </files>
  <action>
**Models (backend/app/models/dane.py):**
Create two models inheriting from Base:

```python
class Department(Base):
    __tablename__ = "dane_departments"
    code: Mapped[str] = mapped_column(String(2), primary_key=True)  # "05", "08", etc.
    name: Mapped[str] = mapped_column(String(100), nullable=False)

class Municipality(Base):
    __tablename__ = "dane_municipalities"
    code: Mapped[str] = mapped_column(String(5), primary_key=True)  # "05001", "08001"
    department_code: Mapped[str] = mapped_column(String(2), ForeignKey("dane_departments.code"), nullable=False)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
```

CRITICAL: Use String type for codes — NEVER Integer (leading zeros).

**Models (backend/app/models/unit_measure.py):**
```python
class UnitOfMeasure(Base):
    __tablename__ = "unit_measures"
    code: Mapped[str] = mapped_column(String(5), primary_key=True)  # "94", "EA", "KGM"
    name: Mapped[str] = mapped_column(String(100), nullable=False)  # "Unidad", "Each", "Kilogramo"
```

**Schemas (backend/app/schemas/dane.py):**
- `DepartmentResponse(code: str, name: str)` with `model_config = {"from_attributes": True}`
- `MunicipalityResponse(code: str, department_code: str, name: str)` with `model_config = {"from_attributes": True}`

**Schemas (backend/app/schemas/unit_measure.py):**
- `UnitOfMeasureResponse(code: str, name: str)` with `model_config = {"from_attributes": True}`

**Router (backend/app/routers/dane.py):**
`APIRouter(prefix="/api/dane", tags=["dane"])`

Three endpoints:
1. `GET /api/dane/departments` → returns all 33 departments ordered by name. Auth required via `Depends(get_current_user)`.
2. `GET /api/dane/municipalities` → accepts optional query params `q: str = ""` and `department_code: str | None = None`. Filter with `ILIKE f"%{q}%"` on name, filter by department_code if provided. Limit 50 results. Order by name. Auth required.
3. `GET /api/dane/municipalities/{code}` → returns single municipality by exact code. 404 if not found. Auth required.

**Router (backend/app/routers/units.py):**
`APIRouter(prefix="/api/units", tags=["units"])`

One endpoint:
1. `GET /api/units` → returns all unit of measure records ordered by name. Auth required.

**Register routers in backend/app/main.py:**
Add imports and `app.include_router()` for both `dane_router` and `units_router`, following existing pattern.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models.dane import Department, Municipality; from app.models.unit_measure import UnitOfMeasure; from app.schemas.dane import DepartmentResponse, MunicipalityResponse; from app.schemas.unit_measure import UnitOfMeasureResponse; from app.routers.dane import router as dr; from app.routers.units import router as ur; print('All imports OK')"</automated>
  </verify>
  <done>Models, schemas, and routers exist and import cleanly. Routers registered in main.py.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration — create tables + seed ALL data + Consumidor Final</name>
  <files>backend/alembic/versions/seed_dane_and_units.py</files>
  <action>
Create a single Alembic migration that:

1. **Creates the 3 reference tables** (dane_departments, dane_municipalities, unit_measures) using `op.create_table()`.

2. **Seeds ALL 33 departments.** Full list:
```
("05","Antioquia"),("08","Atlántico"),("11","Bogotá, D.C."),("13","Bolívar"),
("15","Boyacá"),("17","Caldas"),("18","Caquetá"),("19","Cauca"),("20","Cesar"),
("23","Córdoba"),("25","Cundinamarca"),("27","Chocó"),("41","Huila"),
("44","La Guajira"),("47","Magdalena"),("50","Meta"),("52","Nariño"),
("54","Norte de Santander"),("63","Quindío"),("66","Risaralda"),("68","Santander"),
("70","Sucre"),("73","Tolima"),("76","Valle del Cauca"),("81","Arauca"),
("85","Casanare"),("86","Putumayo"),("88","Archipiélago de San Andrés, Providencia y Santa Catalina"),
("91","Amazonas"),("94","Guainía"),("95","Guaviare"),("97","Vaupés"),("99","Vichada")
```

3. **Seeds ALL ~1,125 municipalities.** Since the full dataset is large, embed it as a Python list of tuples `(code, department_code, name)` directly in the migration file. The executor should download the DANE DIVIPOLA dataset from `https://www.datos.gov.co/resource/gdxc-w37w.json?$limit=2000` using a helper script OR embed a curated version. At minimum, include the capital cities of all 33 departments plus at least 50 of the most populated municipalities. IMPORTANT: The executor should attempt to fetch the full dataset; if not possible, embed the most complete list achievable and add a TODO comment.

4. **Seeds ~30 common UNECE unit of measure codes:**
```
("94","Unidad"),("EA","Cada uno (Each)"),("NAR","Número de artículos"),
("KGM","Kilogramo"),("GRM","Gramo"),("TNE","Tonelada"),("LTR","Litro"),
("MLT","Mililitro"),("MTR","Metro"),("CMT","Centímetro"),("MTK","Metro cuadrado"),
("MTQ","Metro cúbico"),("HUR","Hora"),("DAY","Día"),("MON","Mes"),("ANN","Año"),
("GLL","Galón"),("DZN","Docena"),("SET","Conjunto"),("PR","Par"),
("BX","Caja"),("PK","Paquete"),("BG","Bolsa"),("BO","Botella"),
("CT","Cartón"),("RO","Rollo"),("KWH","Kilovatio hora"),("MWH","Megavatio hora"),
("E54","Viaje"),("ZZ","Mutuamente definido")
```

5. **Seeds "Consumidor Final" client.** This requires the `clients` table to exist, which is created in Plan 02-02. Therefore, create a SEPARATE migration file for Consumidor Final that will run AFTER the clients table migration. Name it clearly: `seed_consumidor_final.py`. This migration should use `op.execute()` with a raw INSERT that checks if the clients table exists, or simply be run after Plan 02-02's migration.

ALTERNATIVE APPROACH (simpler): Skip the Consumidor Final seed in this plan. Instead, add it to Plan 02-02 as a post-migration seed step after the clients table is created. Add a comment in the DANE/units migration noting this.

Use `op.bulk_insert()` for all seed data. The `downgrade()` should `op.drop_table()` in reverse order.
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && python -c "
from sqlalchemy import create_engine, text
import os
engine = create_engine(os.environ.get('DATABASE_URL','postgresql://postgres:postgres@localhost:5432/dian_facturador').replace('+asyncpg',''))
with engine.connect() as c:
    deps = c.execute(text('SELECT count(*) FROM dane_departments')).scalar()
    muns = c.execute(text('SELECT count(*) FROM dane_municipalities')).scalar()
    units = c.execute(text('SELECT count(*) FROM unit_measures')).scalar()
    print(f'Departments: {deps}, Municipalities: {muns}, Units: {units}')
    assert deps == 33, f'Expected 33 departments, got {deps}'
    assert muns >= 50, f'Expected 50+ municipalities, got {muns}'
    assert units >= 25, f'Expected 25+ units, got {units}'
    print('ALL SEED DATA OK')
"</automated>
  </verify>
  <done>Migration runs successfully. dane_departments has 33 rows, dane_municipalities has 50+ rows (ideally ~1,125), unit_measures has ~30 rows. Tables queryable via API endpoints.</done>
</task>

</tasks>

<verification>
1. `alembic upgrade head` runs without errors
2. `GET /api/dane/departments` returns 33 departments
3. `GET /api/dane/municipalities?q=bog` returns Bogotá
4. `GET /api/dane/municipalities?department_code=05` returns Antioquia municipalities
5. `GET /api/units` returns ~30 unit codes
6. All DANE codes are strings with leading zeros preserved
</verification>

<success_criteria>
- Reference tables created and populated via Alembic migration
- API endpoints return correct data with authentication
- DANE codes stored as strings (no leading zero loss)
- Unit of measure codes match UNECE Rec. 20 standard
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-catalogs/02-01-SUMMARY.md`
</output>
